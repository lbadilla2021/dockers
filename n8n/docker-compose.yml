services:
  n8n:
    image: n8nio/n8n:2.30.0
    container_name: n8n

    restart: unless-stopped

    env_file:
      - .env

    ports:
      - "5678:5678"

    volumes:
      - ../../data/n8n:/home/node/.n8n

    networks:
      - lab-network

networks:
  lab-network:
    external: true