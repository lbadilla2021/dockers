TZ=America/Santiago

# Postgres compartido (base "langfuse" ya creada en docker/postgres)
POSTGRES_LANGFUSE_PASSWORD=${PG_LANGFUSE_PASSWORD}

# Redis compartido (docker/redis)
REDIS_AUTH=${REDIS_PASSWORD}

# MinIO compartido (docker/minio) - bucket "langfuse" creado por minio-init
MINIO_ROOT_USER=${MINIO_ROOT_USER}
MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
MINIO_EXTERNAL_ENDPOINT=http://100.94.184.123:9002

# Clickhouse dedicado de este stack
CLICKHOUSE_PASSWORD=${LANGFUSE_CLICKHOUSE_PASSWORD}

# Auth / criptografía de Langfuse
NEXTAUTH_URL=http://100.94.184.123:3000
NEXTAUTH_SECRET=${LANGFUSE_NEXTAUTH_SECRET}
SALT=${LANGFUSE_SALT}
ENCRYPTION_KEY=${LANGFUSE_ENCRYPTION_KEY}

# Bootstrap automático de organización/proyecto/usuario admin (solo primer arranque)
LANGFUSE_INIT_ORG_ID=apex
LANGFUSE_INIT_ORG_NAME=Apex SpA
LANGFUSE_INIT_PROJECT_ID=lab
LANGFUSE_INIT_PROJECT_NAME=Laboratorio
LANGFUSE_INIT_PROJECT_PUBLIC_KEY=${LANGFUSE_INIT_PROJECT_PUBLIC_KEY}
LANGFUSE_INIT_PROJECT_SECRET_KEY=${LANGFUSE_INIT_PROJECT_SECRET_KEY}
LANGFUSE_INIT_USER_EMAIL=${LANGFUSE_INIT_USER_EMAIL}
LANGFUSE_INIT_USER_NAME=Luciano Badilla
LANGFUSE_INIT_USER_PASSWORD=${LANGFUSE_INIT_USER_PASSWORD}
