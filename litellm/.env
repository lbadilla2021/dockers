TZ=America/Santiago

# Postgres compartido (base "litellm" ya creada en docker/postgres)
DATABASE_URL=postgresql://litellm:${PG_LITELLM_PASSWORD}@postgres:5432/litellm

LITELLM_MASTER_KEY=${LITELLM_MASTER_KEY}
LITELLM_SALT_KEY=${LITELLM_SALT_KEY}

# Completar con tus API keys reales antes de levantar el stack
ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
OPENAI_API_KEY=${OPENAI_API_KEY}
